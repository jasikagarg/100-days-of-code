Q25: Write a program to implement a basic calculator using switch-case for +, -, *, /, %.

/*
Sample Test Cases:
Input 1:
4 2 +
Output 1:
6

Input 2:
10 3 %
Output 2:
1

Input 3:
15 5 /
Output 3:
3

*/
  #include<stdio.h>
int main(){
    int operators, sum, difference, product, division, remainder, num1, num2;
    printf("Enter 1 for addition, 2 for subtraction, 3 for division, 4 for remainder and 5 for multiplication: ");
    scanf("%d", &operators);
    printf("Enter 1st number: ");
    scanf("%d", &num1);
    printf("Enter 2nd number: ");
    scanf("%d", &num2);
    switch(operators){
        case 1:
        sum = num1 + num2;
        printf("The sum is %d.\n", sum);
        break;
        case 2:
        difference = num1 - num2;
        printf("The difference is %d.\n", difference);
        break;
        case 3:
        if(num2 != 0){
            division = num1/num2;
            printf("The division is %d.\n", division);
        } else {
            printf("The division is not possible!");
        }
        break;
        case 4:
        remainder = num1 % num2;
        printf("The remainder is %d.\n", remainder);
        break;
        case 5:
        product = num1 * num2;
        printf("The multiplication is %d.\n", product);
        break;
        default:
        printf("Invalid number!");
    }
    return 0;
}
